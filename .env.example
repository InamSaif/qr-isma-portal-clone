# Public URL baked into QR codes and /view links.
# Same pattern as base IOP: iop.<parent-domain>
BASE_URL=https://iop.e-lsmaa.com

# Local Docker (uncomment to override)
# BASE_URL=http://localhost:3017
